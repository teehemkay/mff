---
import { kIndex, kByCountry, kWhyItMatters, kOurPosition, kHowItWorks, kOurRole } from '@packages/etl/constants';

import Head from '../components/Head.astro';
import ShortcutWai from '../components/ShortcutWai.astro';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';

interface Props {
  lang: string;
  currentPage: string;
  currentFAQ: string | null;
}

const { lang, currentPage, currentFAQ } = Astro.props;
---

<!doctype html>
<html lang="en" class="no-js">
  <head>
    <Head {lang} {currentPage} {currentFAQ} />
  </head>
  <body>
    <ShortcutWai {lang} />
    <Navigation {lang} {currentPage} {currentFAQ} />

    {/* Main page content goes here */}
    <slot />

    <a
      href="#returnToTop"
      class="top-link hide js-top"
      aria-label="Scroll back to top of the page"></a>

    <Footer {lang} />

    {/* Common Scripts */}
    <script type="module" is:inline src="/assets/js/go-to-top.js"></script>
    <script type="module" is:inline src="/assets/js/menu-desktop.js"></script>
    <script type="module" is:inline src="/assets/js/dropdown-desktop.js"
    ></script>

    {
      [kHowItWorks, kOurRole].includes(currentFAQ) && (
        <script type="module" is:inline src="/assets/js/expand-collapse.js" />

        <script
          type="module"
          is:inline
          src="/assets/js/copy-url-to-clipboard.js"
        />
      )
    }

    {
      currentFAQ && (
        <script
          type="module"
          is:inline
          src="/assets/js/scroll-faq-into-view.js"
        />
      )
    }

  </body>
</html>
